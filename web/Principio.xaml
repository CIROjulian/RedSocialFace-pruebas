<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="RedSocialFace.views.Principio"
    Title="Principio"
    BackgroundColor="White">

    <ScrollView>
        <VerticalStackLayout Spacing="10" Padding="10">

            <!-- seccion de historias -->
            <ScrollView Orientation="Horizontal" HeightRequest="100">
                <HorizontalStackLayout Spacing="10">
                    <!-- perfiles -->
                    <Frame 
                        WidthRequest="70" HeightRequest="70" 
                        CornerRadius="35"
                        BorderColor="Black" 
                        BackgroundColor="White"
                        Padding="0" 
                        IsClippedToBounds="True">
                        <Image 
                            Source="messi1.png" 
                            Aspect="AspectFill"
                            WidthRequest="70"
                            HeightRequest="70" 
                            Clip="M35,0 A35,35 0 1,1 35,70 A35,35 0 1,1 35,0"
                            HorizontalOptions="Center" 
                            VerticalOptions="Center" />
                    </Frame>

                    <!-- perfiles con borde rojo -->
                    <Frame 
                        WidthRequest="70" HeightRequest="70" 
                        CornerRadius="35"
                        BorderColor="Red" 
                        BackgroundColor="White"
                        Padding="0" 
                        IsClippedToBounds="True">
                        <Image 
                            Source="auron2.png" 
                            Aspect="AspectFill" 
                            WidthRequest="70"
                            HeightRequest="70"
                            Clip="M35,0 A35,35 0 1,1 35,70 A35,35 0 1,1 35,0"
                            HorizontalOptions="Center" 
                            VerticalOptions="Center" />
                    </Frame>

                    <!-- perfiles con borde gris -->
                    <Frame 
                        WidthRequest="70" HeightRequest="70" 
                        CornerRadius="35"
                        BorderColor="Gray" 
                        BackgroundColor="White"
                        Padding="0" 
                        IsClippedToBounds="True">
                        <Image 
                            Source="kun1.png" 
                            Aspect="AspectFill" 
                            WidthRequest="70"
                            HeightRequest="70"
                            Clip="M35,0 A35,35 0 1,1 35,70 A35,35 0 1,1 35,0"
                            HorizontalOptions="Center" 
                            VerticalOptions="Center" />
                    </Frame>

                    <Frame 
                        WidthRequest="70" HeightRequest="70" 
                        CornerRadius="35"
                        BorderColor="Gray" 
                        BackgroundColor="White"
                        Padding="0" 
                        IsClippedToBounds="True">
                        <Image 
                            Source="ibai1.png" 
                            Aspect="AspectFill" 
                            WidthRequest="70"
                            HeightRequest="70"
                            Clip="M35,0 A35,35 0 1,1 35,70 A35,35 0 1,1 35,0"
                            HorizontalOptions="Center" 
                            VerticalOptions="Center" />
                    </Frame>

                    <Frame 
                        WidthRequest="70" HeightRequest="70" 
                        CornerRadius="35"
                        BorderColor="Gray" 
                        BackgroundColor="White"
                        Padding="0" 
                        IsClippedToBounds="True">
                        <Image 
                            Source="perxita1.png" 
                            Aspect="AspectFill" 
                            WidthRequest="70"
                            HeightRequest="70"
                            Clip="M35,0 A35,35 0 1,1 35,70 A35,35 0 1,1 35,0"
                            HorizontalOptions="Center" 
                            VerticalOptions="Center" />
                    </Frame>

                    <Frame 
                        WidthRequest="70" HeightRequest="70" 
                        CornerRadius="35"
                        BorderColor="Gray" 
                        BackgroundColor="White"
                        Padding="0" 
                        IsClippedToBounds="True">
                        <Image 
                            Source="pique1.png" 
                            Aspect="AspectFill" 
                            WidthRequest="70"
                            HeightRequest="70"
                            Clip="M35,0 A35,35 0 1,1 35,70 A35,35 0 1,1 35,0"
                            HorizontalOptions="Center" 
                            VerticalOptions="Center" />
                    </Frame>
                </HorizontalStackLayout>
            </ScrollView>


            <!-- linea de saparacion gris -->
            <BoxView HeightRequest="1" Color="LightGray" Margin="0,5"/>

            <!-- seccion de publicaciones -->
            <VerticalStackLayout Spacing="10">

                <!-- publicacion 1 -->
                <Frame HasShadow="False" Padding="0" Margin="0">
                    <VerticalStackLayout>
                        <!-- encabezado foto a ala izquierda -->
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,*,Auto">
                            <!-- foto de perfil -->
                            <Frame 
                                Grid.Column="0"
                                WidthRequest="40" HeightRequest="40" 
                                CornerRadius="20" 
                                IsClippedToBounds="True" 
                                Padding="0">
                                <Image Source="messi1.png" Aspect="AspectFill"/>
                            </Frame>
                            <!-- nombre del usuario a la izquierda) -->
                            <Label 
                                    Grid.Column="1"
                                    Text="leonel Messi" 
                                    VerticalOptions="Center" 
                                    FontAttributes="Bold" 
                                    Margin="10,0,0,0"/>
                        </Grid>
                        <!-- iamagen -->
                        <Image 
                              Source="messi5.png" 
                              Aspect="AspectFill" 
                              HeightRequest="300"/>
                        <!-- interecciones -->
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,Auto,*,Auto">
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="0">
                                <Image Source="megusta.png" HeightRequest="35" WidthRequest="35"/>
                                <Label Text="10" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="1" Margin="10,0,0,0">
                                <Image Source="like.png" HeightRequest="27" WidthRequest="27"/>
                                <Label Text="5" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="2" Margin="10,0,0,0">
                                <Image Source="comentario.png" HeightRequest="22" WidthRequest="22"/>
                                <Label Text="3" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                        </Grid>

                        <!-- comentario -->
                        <Label 
                              Text="dia tranqui" 
                              Padding="10" 
                              FontSize="14"/>
                        <StackLayout Padding="10" Spacing="5">
                            <!-- Comentario 1 -->
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Frame
                                    WidthRequest="30"
                                    HeightRequest="30"
                                    CornerRadius="15"
                                    IsClippedToBounds="True"
                                    Padding="0">
                                    <!-- Foto del usuario que comenta -->
                                    <Image Source="paul1.png" Aspect="AspectFill"/>
                                </Frame>
                                <!-- Texto del comentario -->
                                <Label Text="de paul: el es solo mio leo me traicionaste" VerticalOptions="Center"/>
                            </StackLayout>

                            <!-- Comentario 2 -->
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Frame
                                    WidthRequest="30"
                                    HeightRequest="30"
                                    CornerRadius="15"
                                    IsClippedToBounds="True"
                                    Padding="0">
                                    <Image Source="pique1.png" Aspect="AspectFill"/>
                                </Frame>
                                <Label Text="pique: yo se enseño a como llevar la relacion" VerticalOptions="Center"/>
                            </StackLayout>

                            <!-- egregar mas comentarios -->
                        </StackLayout>
                    </VerticalStackLayout>
                </Frame>

                <!-- publicacion 2 -->
                <Frame HasShadow="False" Padding="0" Margin="0">
                    <VerticalStackLayout>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,*,Auto">
                            <Frame 
                                Grid.Column="0"
                                WidthRequest="40" HeightRequest="40" 
                                CornerRadius="20" 
                                IsClippedToBounds="True" 
                                Padding="0">
                                <Image Source="kun1.png" Aspect="AspectFill"/>
                            </Frame>
                            <Label 
                                Grid.Column="1"
                                Text="kun" 
                                VerticalOptions="Center" 
                                FontAttributes="Bold" 
                                Margin="10,0,0,0"/>
                        </Grid>
                        <Image 
                              Source="kun2.png" 
                              Aspect="AspectFill" 
                              HeightRequest="300"/>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,Auto,*,Auto">
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="0">
                                <Image Source="megusta.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="7" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="1" Margin="10,0,0,0">
                                <Image Source="like.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="4" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="2" Margin="10,0,0,0">
                                <Image Source="comentario.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="2" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                        </Grid>
                        <Label 
                              Text="Con las estrellitas" 
                              Padding="10" 
                              FontSize="14"/>
                        <StackLayout Padding="10" Spacing="5">
                            <!-- Comentario 1 -->
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Frame
                                    WidthRequest="30"
                                    HeightRequest="30"
                                    CornerRadius="15"
                                    IsClippedToBounds="True"
                                    Padding="0">
                                    <!-- Foto del usuario que comenta -->
                                    <Image Source="bles1.png" Aspect="AspectFill"/>
                                </Frame>
                                <!-- Texto del comentario -->
                                <Label Text="bles: ¡no invitaste a las estrellas!" VerticalOptions="Center"/>
                            </StackLayout>
                            <!-- egregar mas comentarios -->
                        </StackLayout>
                    </VerticalStackLayout>
                </Frame>
                <!-- publicacion 3 -->
                <Frame HasShadow="False" Padding="0" Margin="0">
                    <VerticalStackLayout>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,*,Auto">
                            <Frame 
                                Grid.Column="0"
                                WidthRequest="40" HeightRequest="40" 
                                CornerRadius="20" 
                                IsClippedToBounds="True" 
                                Padding="0">
                                <Image Source="ibai1.png" Aspect="AspectFill"/>
                            </Frame>
                            <Label 
                                Grid.Column="1"
                                Text="Ibai" 
                                VerticalOptions="Center" 
                                FontAttributes="Bold" 
                                Margin="10,0,0,0"/>
                        </Grid>
                        <Image 
                              Source="ibai3.png" 
                              Aspect="AspectFill" 
                              HeightRequest="300"/>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,Auto,*,Auto">
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="0">
                                <Image Source="megusta.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="7" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="1" Margin="10,0,0,0">
                                <Image Source="like.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="4" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="2" Margin="10,0,0,0">
                                <Image Source="comentario.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="2" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                        </Grid>
                        <Label 
                              Text="cantaste muy bien" 
                              Padding="10" 
                              FontSize="14"/>
                        <StackLayout Padding="10" Spacing="5">
                            <!-- Comentario 1 -->
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Frame
                                    WidthRequest="30"
                                    HeightRequest="30"
                                    CornerRadius="15"
                                    IsClippedToBounds="True"
                                    Padding="0">
                                    <!-- Foto del usuario que comenta -->
                                    <Image Source="pique1.png" Aspect="AspectFill"/>
                                </Frame>
                                <!-- Texto del comentario -->
                                <Label Text="pique: ¡llevalo ala velada!" VerticalOptions="Center"/>
                            </StackLayout>
                            <!-- egregar mas comentarios -->
                        </StackLayout>
                    </VerticalStackLayout>
                </Frame>
                <!-- publicacion 4 -->
                <Frame HasShadow="False" Padding="0" Margin="0">
                    <VerticalStackLayout>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,*,Auto">
                            <Frame 
                            Grid.Column="0"
                            WidthRequest="40" HeightRequest="40" 
                            CornerRadius="20" 
                            IsClippedToBounds="True" 
                            Padding="0">
                                <Image Source="pique1.png" Aspect="AspectFill"/>
                            </Frame>
                            <Label 
                            Grid.Column="1"
                            Text="Pique" 
                            VerticalOptions="Center" 
                            FontAttributes="Bold" 
                            Margin="10,0,0,0"/>
                        </Grid>
                        <Image 
                              Source="pique3.png" 
                              Aspect="AspectFill" 
                              HeightRequest="300"/>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,Auto,*,Auto">
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="0">
                                <Image Source="megusta.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="7" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="1" Margin="10,0,0,0">
                                <Image Source="like.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="4" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="2" Margin="10,0,0,0">
                                <Image Source="comentario.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="2" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                        </Grid>
                        <Label 
                              Text="Aqui con el amor de mi vida" 
                              Padding="10" 
                              FontSize="14"/>
                        <StackLayout Padding="10" Spacing="5">
                            <!-- Comentario 1 -->
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Frame
                                    WidthRequest="30"
                                    HeightRequest="30"
                                    CornerRadius="15"
                                    IsClippedToBounds="True"
                                    Padding="0">
                                    <!-- Foto del usuario que comenta -->
                                    <Image Source="kira1.png" Aspect="AspectFill"/>
                                </Frame>
                                <!-- Texto del comentario -->
                                <Label Text="skakira: fan de su relacion" VerticalOptions="Center"/>
                            </StackLayout>
                            <!-- egregar mas comentarios -->
                        </StackLayout>
                    </VerticalStackLayout>
                </Frame>
                <!-- publicacion 5 -->
                <Frame HasShadow="False" Padding="0" Margin="0">
                    <VerticalStackLayout>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,*,Auto">
                            <Frame 
                                Grid.Column="0"
                                WidthRequest="40" HeightRequest="40" 
                                CornerRadius="20" 
                                IsClippedToBounds="True" 
                                Padding="0">
                                <Image Source="messi1.png" Aspect="AspectFill"/>
                            </Frame>
                            <Label 
                                Grid.Column="1"
                                Text="Messi" 
                                VerticalOptions="Center" 
                                FontAttributes="Bold" 
                                Margin="10,0,0,0"/>
                        </Grid>
                        <Image 
                              Source="messi3.png" 
                              Aspect="AspectFill" 
                              HeightRequest="300"/>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,Auto,*,Auto">
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="0">
                                <Image Source="megusta.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="7" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="1" Margin="10,0,0,0">
                                <Image Source="like.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="4" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="2" Margin="10,0,0,0">
                                <Image Source="comentario.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="2" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                        </Grid>
                        <Label 
                              Text="dia 10000 siendo el campeon del mundo" 
                              Padding="10" 
                              FontSize="14"/>
                        <StackLayout Padding="10" Spacing="5">
                            <!-- Comentario 1 -->
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Frame
                                    WidthRequest="30"
                                    HeightRequest="30"
                                    CornerRadius="15"
                                    IsClippedToBounds="True"
                                    Padding="0">
                                    <!-- Foto del usuario que comenta -->
                                    <Image Source="cr7.png" Aspect="AspectFill"/>
                                </Frame>
                                <!-- Texto del comentario -->
                                <Label Text="CR7: soy el mejor del mundo" VerticalOptions="Center"/>
                            </StackLayout>
                            <!-- egregar mas comentarios -->
                        </StackLayout>
                    </VerticalStackLayout>
                </Frame>
                <!-- publicacion 6 -->
                <Frame HasShadow="False" Padding="0" Margin="0">
                    <VerticalStackLayout>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,*,Auto">
                            <Frame 
                                Grid.Column="0"
                                WidthRequest="40" HeightRequest="40" 
                                CornerRadius="20" 
                                IsClippedToBounds="True" 
                                Padding="0">
                                <Image Source="pique1.png" Aspect="AspectFill"/>
                            </Frame>
                            <Label 
                                Grid.Column="1"
                                Text="Pique" 
                                VerticalOptions="Center" 
                                FontAttributes="Bold" 
                                Margin="10,0,0,0"/>
                        </Grid>
                        <Image 
                              Source="pique4.png" 
                              Aspect="AspectFill" 
                              HeightRequest="300"/>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,Auto,*,Auto">
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="0">
                                <Image Source="megusta.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="7" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="1" Margin="10,0,0,0">
                                <Image Source="like.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="4" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="2" Margin="10,0,0,0">
                                <Image Source="comentario.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="2" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                        </Grid>
                        <Label 
                              Text="noche de netflix" 
                              Padding="10" 
                              FontSize="14"/>
                        <StackLayout Padding="10" Spacing="5">
                            <!-- Comentario 1 -->
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Frame
                                    WidthRequest="30"
                                    HeightRequest="30"
                                    CornerRadius="15"
                                    IsClippedToBounds="True"
                                    Padding="0">
                                    <!-- Foto del usuario que comenta -->
                                    <Image Source="kira1.png" Aspect="AspectFill"/>
                                </Frame>
                                <!-- Texto del comentario -->
                                <Label Text="skakira: que la pasen bien!!!!" VerticalOptions="Center"/>
                            </StackLayout>
                            <!-- egregar mas comentarios -->
                        </StackLayout>
                    </VerticalStackLayout>
                </Frame>
                <!-- publicacion 7 -->
                <Frame HasShadow="False" Padding="0" Margin="0">
                    <VerticalStackLayout>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,*,Auto">
                            <Frame 
                                Grid.Column="0"
                                WidthRequest="40" HeightRequest="40" 
                                CornerRadius="20" 
                                IsClippedToBounds="True" 
                                Padding="0">
                                <Image Source="kun1.png" Aspect="AspectFill"/>
                            </Frame>
                            <Label 
                                Grid.Column="1"
                                Text="kun" 
                                VerticalOptions="Center" 
                                FontAttributes="Bold" 
                                Margin="10,0,0,0"/>
                        </Grid>
                        <Image 
                              Source="kun3.png" 
                              Aspect="AspectFill" 
                              HeightRequest="300"/>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,Auto,*,Auto">
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="0">
                                <Image Source="megusta.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="7" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="1" Margin="10,0,0,0">
                                <Image Source="like.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="4" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="2" Margin="10,0,0,0">
                                <Image Source="comentario.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="2" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                        </Grid>
                        <Label 
                              Text="noche tranquila solo se toma agua" 
                              Padding="10" 
                              FontSize="14"/>
                        <StackLayout Padding="10" Spacing="5">
                            <!-- Comentario 1 -->
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Frame
                                    WidthRequest="30"
                                    HeightRequest="30"
                                    CornerRadius="15"
                                    IsClippedToBounds="True"
                                    Padding="0">
                                    <!-- Foto del usuario que comenta -->
                                    <Image Source="bles1.png" Aspect="AspectFill"/>
                                </Frame>
                                <!-- Texto del comentario -->
                                <Label Text="bles: que noche de locos se tomo de todo menos agua" VerticalOptions="Center"/>
                            </StackLayout>
                            <!-- egregar mas comentarios -->
                        </StackLayout>
                    </VerticalStackLayout>
                </Frame>
                <!--publicacion 8-->
                <Frame HasShadow="False" Padding="0" Margin="0">
                    <VerticalStackLayout>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,*,Auto">
                            <Frame 
                                Grid.Column="0"
                                WidthRequest="40" HeightRequest="40" 
                                CornerRadius="20" 
                                IsClippedToBounds="True" 
                                Padding="0">
                                <Image Source="auron3.png" Aspect="AspectFill"/>
                            </Frame>
                            <Label 
                                Grid.Column="1"
                                Text="auron" 
                                VerticalOptions="Center" 
                                FontAttributes="Bold" 
                                Margin="10,0,0,0"/>
                        </Grid>
                        <Image 
                          Source="auron5.png" 
                          Aspect="AspectFill" 
                          HeightRequest="300"/>
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,Auto,*,Auto">
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="0">
                                <Image Source="megusta.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="7" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="1" Margin="10,0,0,0">
                                <Image Source="like.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="4" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="2" Margin="10,0,0,0">
                                <Image Source="comentario.png" WidthRequest="25" HeightRequest="25"/>
                                <Label Text="2" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                        </Grid>
                        <Label 
                          Text="no se porque fui avento de mierda" 
                          Padding="10" 
                          FontSize="14"/>
                        <StackLayout Padding="10" Spacing="5">
                            <!-- Comentario 1 -->
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Frame
                                    WidthRequest="30"
                                    HeightRequest="30"
                                    CornerRadius="15"
                                    IsClippedToBounds="True"
                                    Padding="0">
                                    <!-- Foto del usuario que comenta -->
                                    <Image Source="ibai1.png" Aspect="AspectFill"/>
                                </Frame>
                                <!-- Texto del comentario -->
                                <Label Text="Ibai: payasoooo" VerticalOptions="Center"/>
                            </StackLayout>

                            <!-- Comentario 2 -->
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Frame
                                    WidthRequest="30"
                                    HeightRequest="30"
                                    CornerRadius="15"
                                    IsClippedToBounds="True"
                                    Padding="0">
                                    <Image Source="perxita1.png" Aspect="AspectFill"/>
                                </Frame>
                                <Label Text="Perxita: no pagaste la sena a noche" VerticalOptions="Center"/>
                            </StackLayout>

                            <!-- egregar mas comentarios -->
                        </StackLayout>
                    </VerticalStackLayout>
                </Frame>
                <!--publicacion 8-->
                <Frame HasShadow="False" Padding="10" Margin="5" CornerRadius="10" BorderColor="LightGray">
                    <VerticalStackLayout>
                        <!-- Sección de usuario -->
                        <Grid Padding="5" ColumnDefinitions="Auto,*,Auto" VerticalOptions="Center">
                            <Frame 
                                Grid.Column="0"
                                WidthRequest="40" HeightRequest="40" 
                                CornerRadius="20" 
                                IsClippedToBounds="True" 
                                Padding="0">
                                <Image Source="messi1.png" Aspect="AspectFill"/>
                            </Frame>
                            <Label 
                                Grid.Column="1"
                                Text="Leonel Messi" 
                                VerticalOptions="Center" 
                                FontAttributes="Bold" 
                                Margin="10,0,0,0"/>
                        </Grid>

                        <!-- iamgen de la publicacion acomodada -->
                        <Frame Padding="0" CornerRadius="10" IsClippedToBounds="False">
                            <Image 
                              Source="messi2.png" 
                              Aspect="AspectFit" 
                              HeightRequest="300"/>
                        </Frame>
                        <!-- interaciones -->
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,Auto,*,Auto" VerticalOptions="Center">
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="0">
                                <Image Source="megusta.png" WidthRequest="22" HeightRequest="22"/>
                                <Label Text="7" FontSize="14" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="1" Margin="10,0,0,0">
                                <Image Source="like.png" WidthRequest="22" HeightRequest="22"/>
                                <Label Text="4" FontSize="14" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="2" Margin="10,0,0,0">
                                <Image Source="comentario.png" WidthRequest="22" HeightRequest="22"/>
                                <Label Text="2" FontSize="14" VerticalOptions="Center"/>
                            </StackLayout>
                        </Grid>
                        <!-- comentario -->
                        <Label 
                          Text="que miras bobo?." 
                          Padding="10,5,10,10" 
                          FontSize="14"/>
                        <StackLayout Padding="10" Spacing="5">
                            <!-- Comentario 1 -->
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Frame
                                    WidthRequest="30"
                                    HeightRequest="30"
                                    CornerRadius="15"
                                    IsClippedToBounds="True"
                                    Padding="0">
                                    <!-- Foto del usuario que comenta -->
                                    <Image Source="cr7.png" Aspect="AspectFill"/>
                                </Frame>
                                <!-- Texto del comentario -->
                                <Label Text="CR7: te regalaron el mundial" VerticalOptions="Center"/>
                            </StackLayout>

                            <!-- Comentario 2 -->
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Frame
                                    WidthRequest="30"
                                    HeightRequest="30"
                                    CornerRadius="15"
                                    IsClippedToBounds="True"
                                    Padding="0">
                                    <Image Source="paul1.png" Aspect="AspectFill"/>
                                </Frame>
                                <Label Text="De Paul:mira se enojo la niña de penaldo" VerticalOptions="Center"/>
                            </StackLayout>

                            <!-- egregar mas comentarios -->
                        </StackLayout>
                    </VerticalStackLayout>
                </Frame>
                <!-- publicacion 1 -->
                <Frame HasShadow="False" Padding="0" Margin="0">
                    <VerticalStackLayout>
                        <!-- Encabezado (foto + nombre) -->
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,*,Auto">
                            <Frame 
                                Grid.Column="0"
                                WidthRequest="40" HeightRequest="40" 
                                CornerRadius="20" 
                                IsClippedToBounds="True" 
                                Padding="0">
                                <Image Source="messi1.png" Aspect="AspectFill"/>
                            </Frame>
                            <Label 
                                Grid.Column="1"
                                Text="Leonel Messi" 
                                VerticalOptions="Center" 
                                FontAttributes="Bold" 
                                Margin="10,0,0,0"/>
                        </Grid>
                        <!-- Imagen principal del post -->
                        <Image 
                            Source="messi11.png" 
                            Aspect="AspectFill" 
                            HeightRequest="300"/>
                        <!-- Interacciones -->
                        <Grid Padding="10" ColumnDefinitions="Auto,Auto,Auto,*,Auto">
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="0">
                                <Image Source="megusta.png" HeightRequest="35" WidthRequest="35"/>
                                <Label Text="10" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="1" Margin="10,0,0,0">
                                <Image Source="like.png" HeightRequest="27" WidthRequest="27"/>
                                <Label Text="5" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" Spacing="5" Grid.Column="2" Margin="10,0,0,0">
                                <Image Source="comentario.png" HeightRequest="22" WidthRequest="22"/>
                                <Label Text="3" FontSize="16" VerticalOptions="Center"/>
                            </StackLayout>
                        </Grid>
                        <Label 
                            Text="dedicado para mis fans" 
                            Padding="10" 
                            FontSize="14"/>
                    </VerticalStackLayout>
                </Frame>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>